Grid Fitter
Maintainer: Jonathan Williams

INTRODUCTION

Code to fit an n-dimensional grid of data points with a parabola or paraboloid (depending on the number of free parameters) and find the minimum/maximum.  Currently implemented for data with 2 or 3 free parameters.  Example data is included in the 'sample_2par.txt' and 'sample_3par.txt' files.


FITTING FUNCTIONS USED
2 free parameters: f(x,y) = a1*x^2 + a2*y^2 + a3*x*y + a4*x + a5*y + a6
3 free parameters: f(x,y,z) = a1*x^2 + a2*y^2 + a3*z^2 + a4*x*y + a5*x*z + a6*y*z + a7*x + a8*y + a9*z + a10


INSTRUCTIONS

Use 'make' to compile.  Requires gcc compiler.
To run the program from anywhere, move the resulting 'gridfit' executable to any directory under your $PATH environment variable.
Tested and working on Ubuntu 14.04 and Scientific Linux/CentOS 6.


CONTRIBUTIONS

J. Williams - core logic
K. Starosta - linear equation solver code for determining fit parameter values
